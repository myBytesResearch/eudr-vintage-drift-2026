jupyter>=1.0.0
numpy>=1.26.0
pandas>=2.0.0
matplotlib>=3.8.0
seaborn>=0.13.0
# Optional, for real raster I/O (uncomment when running on real data):
# rasterio>=1.4
# rioxarray>=0.15
# geopandas>=0.14
# shapely>=2.0

# Optional, for auto-loading EE_PROJECT and other secrets from .env:
python-dotenv>=1.0.0
